Installation guide

Create a database and import the sql.sql file.
open web URL where the site has been installed
Installation page will open and then type the basic details.

Modify the .htaccess file for URL rewriting. If your site is installed under any subdirectory then replace 
RewriteBase /

to 

RewriteBase /{ subdirectory name}

-------------------

All done :)